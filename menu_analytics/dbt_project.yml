
name: 'menu_analytics'
version: '1.0.0'

profile: 'menu_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         
  - "target"
  - "dbt_packages"


models:
  menu_analytics:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    dimensions:
      +materialized: table
    marts:
      +materialized: table

